{% extends "layout.twig" %}

{% block breadcrumbs %}
    {% set breadcrumbsList = [{url: url({controller: 'profile', action: 'index', id: evemt.eid}, 'admin'), label: 'Edycja profilu "'~ user.firstname ~' <'~ user.username ~'> '~ user.lastname ~'"'}] %}
    {{ cms.breadcrumbs(breadcrumbsList) }}
{% endblock %}
    
{% block content %}                   

<div class="right-side">
    <div class="content">
        <h1>Edycja profilu "{{ user.firstname }} &lt;{{ user.username }}&gt; {{ user.lastname }}": </h1>
        {{ cmsForm|raw }}

        <script type="text/javascript">
        // <[[!CDATA
            $(document).ready(function(){

                $('.tabs').tabs();  

                $('.ui-tabs-panel').each(function () {
                        var id = $(this).has('.error').attr('id');
                        var count = $(this).find('.error').size();
                        $('a[href="#'+id+'"]').append(' <em>('+ count +')</em>');
                });

            });
        // ]]>
        </script>
    </div>
</div>

{% endblock %}