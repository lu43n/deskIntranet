{% extends "layout.twig" %}

{% block breadcrumbs %}
    {% set breadcrumbsList = [] %}

    {% set breadcrumbsList = breadcrumbsList|merge([{url: url({controller: 'settings', action: 'edit', id: settingsGroup.fid}, 'admin', true), label: settingsGroup.translation.title}]) %}
    {% set breadcrumbsList = breadcrumbsList|merge([{url: url({controller: 'settings'}, 'admin', true), label: 'Ustawienia'}]) %}

    {{ cms.breadcrumbs(breadcrumbsList) }}
{% endblock %}

{% block helpbox %}
    {% include 'settings/_helpBox.twig' %}     
{% endblock %}

{% block content %}         
<div class="right-side">
    <div class="content">
        <h1>Ustawienia - {{ settingsGroup.translation.title }}</h1>
        {{ settingsForm|raw }}
    </div>
</div>          
{% endblock %}