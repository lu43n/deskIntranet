{% extends "layout.twig" %}

{% block breadcrumbs %}
    {% set breadcrumbsList = [{url: url({controller: 'dictionary', action: 'add', id: word.did}, 'admin'), label: 'Dodawanie tłumaczenia'}] %}
    {% set breadcrumbsList = breadcrumbsList|merge([{url: url({controller: 'dictionary'}, 'admin', true), label: 'Słownik'}]) %}
    {{ cms.breadcrumbs(breadcrumbsList) }}
{% endblock %}
    
{% block helpbox %}
    {% include 'dictionary/_helpBox.twig' %}
{% endblock %}

{% block content %}                   

<div class="right-side">
    <div class="content">
        <h1>Dodawanie tłumaczenia: </h1>
        {{ dictionaryForm|raw }}

        <script type="text/javascript">
        // <[[!CDATA
            $(document).ready(function(){

                $('.tabs').tabs();  

                $('.ui-tabs-panel').each(function () {
                        var id = $(this).has('.error').attr('id');
                        var count = $(this).find('.error').size();
                        $('a[href="#'+id+'"]').append(' <em>('+ count +')</em>');
                });

            });
        // ]]>
        </script>
    </div>
</div>

{% endblock %}