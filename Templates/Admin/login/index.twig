{% extends "layout.twig" %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ baseUrl }}/css/admin/login.css" type="text/css" />
    
    <style type="text/css">
        #main .form fieldset .field label {
            float:none;
            text-align: left;
        }
                
        #main .form fieldset .field input, #main .content .form fieldset .field textarea {
            width: 97%;
            max-width: none;
        }
        
    </style>
    
    {{ parent() }}
{% endblock %}

{% block content %}                   

<div class="right-side">
    <div class="content">
        <h1>Logowanie</h1>
        {{ loginForm|raw }}
    </div>
</div>
{% endblock %}