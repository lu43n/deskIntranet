<div class="help_box_start"></div>
<div class="help_box">
    <h2 style="padding-top: 0;">Akcje</h2>
    <ul>
        <li>
            <a href="{{ url({ controller: 'chat'}, 'admin') }}" style="font-weight:bold;"><img src="{{ baseUrl }}/images/admin/refresh_small.png" alt="Odświerz listę" /> Odświerz listę</a>
        </li>
        {% if request.getActionName() == 'history' %}
        <li>
            <a href="{{ url({controller: 'chat', action: 'index'}, 'admin', true) }}" style="font-weight:bold;"><img src="{{ baseUrl }}/images/admin/back_small.png" alt="Powrót" /> Powrót do listy</a>
        </li>
        {% endif %}
        <li id="delete-chats" style="display: none;">
            <a href="{{ url({controller: 'chat', action: 'delete-chats'}, 'admin') }}" style="font-weight:bold;"><img src="{{ baseUrl }}/images/admin/delete_small.png" alt="Usuń zaznaczone" /> Usuń zaznaczone</a>
        </li>
    </ul>
</div>
<div class="help_box_end"></div> 

<div class="help_box_start"></div>
<div class="help_box">
    <h2 style="padding-top: 0;">Szukaj</h2>
    <form action="{{ url({controller: 'chat', action: 'search'}, 'admin', true) }}" method="get">
    <ul class="searchHelpBox">
        <li>
            <input type="text" name="keyword" class="searchQuery" />
        </li>
    </ul>
    </form>        
</div>
<div class="help_box_end"></div>    